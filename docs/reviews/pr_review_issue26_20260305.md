# PR レビューレポート

**PR/ブランチ**: issue26
**レビュー日時**: 2026-03-05
**変更規模**: +148 / -0 / 6ファイル

---

## 🎯 変更の概要

`ConnectivityManager` と `NetworkCallback` を `callbackFlow` でラップし、ネットワーク接続状態を `Flow<Boolean>` として公開する `NetworkMonitor` インターフェースと実装を追加する。`PortalViewModel` に注入して `isOnline: StateFlow<Boolean>` として公開し、次の Issue（オフライン Snackbar）で UI 側から利用できる基盤を整える。

**変更種別**:
- [x] 新機能 (Feature)

---

## ✅ マージ判定

> **APPROVE**

`callbackFlow` + `awaitClose` の正しいパターンで実装されており、リソースリークのリスクがない。`distinctUntilChanged` による重複抑制、`onLost` での再確認ロジック、Hilt `@Singleton` スコープ管理、`initialValue = true` による楽観的初期値など、すべての設計判断が適切。PortalViewModelTest も `NetworkMonitor` mock を追加してカバレッジを維持している。

---

## 📁 変更ファイル一覧

| ファイル | 変更種別 | +行 | -行 | 懸念度 |
|---------|---------|-----|-----|--------|
| `data/network/NetworkMonitor.kt` | Added | +13 | - | 🟢 問題なし |
| `data/network/ConnectivityNetworkMonitor.kt` | Added | +63 | - | 🟢 問題なし |
| `di/NetworkMonitorModule.kt` | Added | +18 | - | 🟢 問題なし |
| `AndroidManifest.xml` | Modified | +1 | - | 🟢 問題なし |
| `ui/portal/PortalViewModel.kt` | Modified | +10 | - | 🟢 問題なし |
| `ui/portal/PortalViewModelTest.kt` | Modified | +20 | - | 🟢 問題なし |

---

## 🔍 詳細レビュー

### NetworkMonitor.kt

#### 変更の意図
インターフェース分離原則に従い、ネットワーク監視の抽象を定義。テスト時に `ConnectivityNetworkMonitor` を使わずにモックで差し替えられるようにする。

#### 指摘事項
なし。シンプルで意図が明確。

---

### ConnectivityNetworkMonitor.kt

#### 変更の意図
`ConnectivityManager.NetworkCallback` の3つのコールバック（`onAvailable`、`onLost`、`onCapabilitiesChanged`）を `callbackFlow` でラップし、`Flow<Boolean>` に変換する。

#### 良い点

**[🟢 G-1] `awaitClose` でリソースリーク防止** (`ConnectivityNetworkMonitor.kt:57`)
```kotlin
awaitClose { connectivityManager.unregisterNetworkCallback(callback) }
```
Flow がキャンセルされたとき（ViewModel の scope 終了時）に `NetworkCallback` を確実に解除している。

**[🟢 G-2] `onLost` で複数ネットワーク対応** (`ConnectivityNetworkMonitor.kt:40-42`)
```kotlin
override fun onLost(network: Network) {
    trySend(connectivityManager.isCurrentlyConnected())
}
```
単純に `false` を emit せず、`activeNetwork` を再確認することでモバイル↔Wi-Fi 切り替え時の誤検知を防ぐ設計が優秀。

**[🟢 G-3] `distinctUntilChanged` で不要な更新を抑制** (`ConnectivityNetworkMonitor.kt:58`)
```kotlin
}.distinctUntilChanged()
```
`onCapabilitiesChanged` は接続状態変化以外でも呼ばれるため、同値フィルタリングで下流の再描画を抑制している。

#### 指摘事項
なし。

---

### NetworkMonitorModule.kt

#### 変更の意図
Hilt で `NetworkMonitor` インターフェースと `ConnectivityNetworkMonitor` 実装をバインドするモジュール。`@Singleton` でアプリ全体で1インスタンスを共有。

#### 指摘事項
なし。`SingletonComponent` + `@Binds` の標準的な実装。

---

### AndroidManifest.xml

#### 変更の意図
`ConnectivityManager` を使うために必要な `ACCESS_NETWORK_STATE` 権限を追加。

#### 指摘事項
なし（既に `INTERNET` 権限は宣言済み）。

---

### PortalViewModel.kt

#### 変更の意図
`NetworkMonitor` を注入し、`isOnline: StateFlow<Boolean>` として公開。`SharingStarted.WhileSubscribed(5_000)` により購読者がいる間だけ上流 Flow を購読する。

#### 良い点

**[🟢 G-4] `initialValue = true` で楽観的初期値** (`PortalViewModel.kt:42`)
```kotlin
initialValue = true,
```
アプリ起動直後は接続ありと仮定することで、初回 API 呼び出しがオフライン判定でブロックされるのを防ぐ。オフラインであれば `ConnectivityNetworkMonitor` が即座に `false` を emit する。

#### 指摘事項
なし。

---

### PortalViewModelTest.kt

#### 変更の意図
`NetworkMonitor` のモックを追加し、`createViewModel()` の引数と `setUp()` のデフォルト振る舞いを更新。`isOnline` の値が ViewModel に正しく反映されることを確認するテストを追加。

#### 良い点

**[🟢 G-5] `flowOf(true)` で軽量なデフォルトモック**
```kotlin
every { networkMonitor.isOnline } returns flowOf(true)
```
既存テストへの影響を最小化しながら、デフォルト値を明示的に設定している。

---

## ⚠️ 影響範囲

**このPRの変更が影響する箇所**:
- `PortalScreen.kt` — `isOnline` を `collectAsState()` で受け取ることが可能になる（次の Issue 27 で使用予定）
- `PortalViewModelTest.kt` — `createViewModel()` シグネチャ変更により、既存テストすべてに `networkMonitor` が渡される

**破壊的変更 (Breaking Change)**: なし（`PortalViewModel` のコンストラクタ変更は Hilt が自動注入するため UI 側への影響なし）

---

## 🧪 テスト確認

| テスト項目 | 状態 |
|-----------|------|
| 既存テストがパスするか | ✅ BUILD SUCCESSFUL |
| 新機能のテストが追加されているか | ✅ `isOnline - NetworkMonitorのisOnlineの値を反映する` |
| エッジケースのテスト | 🟡 オフライン→オンライン変化のテストは次 Issue で追加可 |

---

## 💬 レビューコメント（コピペ用）

**全体コメント**:
```
レビューしました。

指摘事項なし。APPROVE します。

callbackFlow + awaitClose の正しい実装、onLost での複数ネットワーク対応、
distinctUntilChanged による重複抑制など、設計判断がすべて適切です。
PortalViewModel への isOnline 公開も次 Issue の Snackbar 実装の良い基盤になっています。
```

---

## ✅ チェックリスト

- [x] Critical issueがすべて解決されている（指摘なし）
- [x] テストが追加・更新されている
- [x] 破壊的変更がある場合、マイグレーション手順が記載されている（破壊的変更なし）
- [x] セルフレビュー済み

---
*Generated by Claude Code / pr-review skill*
