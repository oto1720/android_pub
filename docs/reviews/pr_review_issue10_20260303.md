# PR レビューレポート

**PR/ブランチ**: issue10 → main
**レビュー日時**: 2026-03-03
**変更規模**: +105 / -0 / 2ファイル（コードファイルのみ）

---

## 🎯 変更の概要

issue #10「GetTrendArticlesUseCase 実装」の対応。トレンド記事取得のビジネスロジックを UseCase に切り出す。`tag = null` のときは Qiita トレンド記事、`tag` 指定のときは Qiita API に `tag:{タグ名}` クエリを送信しつつクライアント側でも完全一致フィルタリングを実施。`CancellationException` を適切に再スローし、コルーチンキャンセルの協調性を維持。

**変更種別**:
- [x] 新機能 (Feature) — UseCase層の実装

---

## ✅ マージ判定

> **APPROVE**

issue #10 の完了条件（UseCase が正常に記事リストを返すこと）を満たしている。`CancellationException` の再スロー、`operator fun invoke` パターン、9ケースのユニットテストなど設計・品質ともに適切。

---

## 📁 変更ファイル一覧

| ファイル | 変更種別 | +行 | -行 | 懸念度 |
|---------|---------|-----|-----|--------|
| `domain/usecase/GetTrendArticlesUseCase.kt` | Added | +55 | - | 🟡 軽微（設計トレードオフあり） |
| `test/domain/usecase/GetTrendArticlesUseCaseTest.kt` | Added | +50 | - | 🟢 問題なし |

---

## 🔍 詳細レビュー

### domain/usecase/GetTrendArticlesUseCase.kt — 🟡 軽微

#### 変更の意図
Repository を DI で受け取り、API 取得とタグフィルタリングのビジネスロジックをカプセル化する UseCase。

#### 良い点
- `operator fun invoke` による Clean Architecture の UseCase 慣例に準拠 ✅
- `CancellationException` を明示的に再スローしてコルーチンキャンセルを伝播 ✅
- `refreshPortalArticles` 失敗時に `observePortalArticles` を呼ばないガード処理 ✅
- クライアント側フィルタリングの意図を KDoc の `NOTE:` に明記 ✅
- `ignoreCase = true` で大文字小文字を無視したタグマッチ ✅

```kotlin
// ✅ CancellationException の適切な処理
} catch (e: CancellationException) {
    throw e  // コルーチンキャンセルは伝播させる
} catch (e: Exception) {
    Result.failure(e)
}
```

#### 指摘事項

**[🟡 m-1] `observePortalArticles().first()` と `refreshPortalArticles` の完了タイミング** (`GetTrendArticlesUseCase.kt:38`)

`refreshPortalArticles` は `deleteByStatus` → `insertAll` を順次実行するが、DBへの書き込み完了と `.first()` の emit タイミングが論理的には直列（suspend fun が完了してから next statement）のため、実運用上はほぼ問題ない。ただし Room の実装依存である点は留意が必要。

```kotlin
// 現在の実装（実用上は問題なし）
val refreshResult = repository.refreshPortalArticles(...)  // 完了を待つ
// ↓ refreshPortalArticles が return した後に Room の Flow が emit される
val articles = repository.observePortalArticles().first()
```

---

**[🟡 m-2] `observePortalArticles` が Flow を返すのに `.first()` でワンショット化** (`GetTrendArticlesUseCase.kt:38`)

UseCase が `Result<List<ArticleEntity>>` を返す現設計は呼び出し時点のスナップショットしか返せない。将来 UI がリアルタイム更新を必要とする場合は設計変更が必要になる。

```kotlin
// 💡 将来の拡張案: Flow を返す設計
fun invoke(tag: String? = null): Flow<List<ArticleEntity>> =
    repository.observePortalArticles()
        .map { articles -> articles.filterByTag(tag) }
// refresh は ViewModel 側で別途呼び出す
```

> **現在のユースケース（ワンショット取得）では問題なし。将来要件に応じて検討。**

---

### test/domain/usecase/GetTrendArticlesUseCaseTest.kt — 🟢 問題なし

#### 変更の意図
MockK で `ArticleRepository` をモックし、UseCase の振る舞いをユニットテスト。

#### 良い点
- `tag = null` 正常系・`tag` 指定正常系・エラー系の3軸で網羅 ✅
- スペース入りタグ（` Android , Kotlin `）のエッジケースを確認 ✅
- DB エラー時の `Result.failure` 返却を検証 ✅
- API エラー時に `observePortalArticles` を呼ばないことを `coVerify(exactly = 0)` で検証 ✅
- `createEntity` ヘルパーでテストデータを集約 ✅
- タグフィルタの検証に `toSet()` を使い順序非依存にしている ✅

```kotlin
// ✅ 順序に依存しない検証
assertEquals(setOf("1", "3"), result.getOrNull()?.map { it.id }?.toSet())
```

---

## ⚠️ 影響範囲

**このPRの変更が影響する箇所**:
- `GetTrendArticlesUseCase` は後続の `PortalViewModel`（issue #11 以降）から `@Inject constructor` で注入される予定
- Hilt モジュールへの追加不要（`@Inject constructor` のみで自動バインド）

**破壊的変更**: なし（新規追加のみ）

---

## 🧪 テスト確認

| テスト項目 | 状態 |
|-----------|------|
| `tag = null` でトレンド記事を取得 | ✅ |
| `tag = null` で0件の正常系 | ✅ |
| `tag` 指定で API クエリが `"tag:xxx"` 形式になる | ✅ |
| `tag` 指定でタグ一致記事のみ返す | ✅ |
| 大文字小文字無視フィルタリング | ✅ |
| スペース入りタグのフィルタリング | ✅ |
| API エラー時に `Result.failure` | ✅ |
| API エラー時に `observePortalArticles` 非呼び出し | ✅ |
| DB エラー時に `Result.failure` | ✅ |

---

## 💬 レビューコメント（コピペ用）

**全体コメント**:
```
レビューしました。APPROVE です 👍

UseCase としての責務が明確で、タグフィルタリングのビジネスロジックが
適切にカプセル化されています。
CancellationException の再スローによるコルーチン協調キャンセルの維持、
APIエラー時の副作用ガードなど、コルーチン周りの扱いが丁寧です。

テストも9ケースで主要パスを網羅しており、スペース入りタグ・DBエラーの
エッジケースまでカバーされています。

将来的に UI がリアルタイム更新を必要とする場合は
Flow を返す設計への移行を検討してください（m-2）。

詳細は docs/reviews/pr_review_issue10_20260303.md を参照してください。
```

---

## ✅ チェックリスト

- [x] `GetTrendArticlesUseCase` が作成されている
- [x] タグ別フィルタリングのロジックが実装されている（API クエリ + クライアント側完全一致）
- [x] `Result<List<ArticleEntity>>` でラップして返す
- [x] `CancellationException` を再スローしてコルーチンキャンセルを伝播
- [x] `operator fun invoke` パターンで UseCase の慣例に準拠
- [x] ユニットテストが追加されている（9ケース）
- [x] 大文字小文字無視・スペース入りタグのエッジケースをテスト

---
*Generated by Claude Code / pr-review skill*
