# PR レビューレポート

**PR/ブランチ**: issue8 → main ([#44](https://github.com/CyberAgentHack/dojo2026_android_otozu_kotaro/pull/44))
**レビュー日時**: 2026-03-03
**変更規模**: +229 / -4 / 6ファイル（コードファイルのみ）

---

## 🎯 変更の概要

issue #8「Qiita API Service 定義」の対応。Retrofit を使って Qiita API v2 の `GET /api/v2/items` エンドポイントを定義し、レスポンスを受け取る `QiitaArticleDto` を作成。`NetworkModule` に `QiitaApiService` を登録し、Hilt 経由で注入可能にする。併せて `MockWebServer` を使った統合テストも追加。

**変更種別**:
- [x] 新機能 (Feature) — Qiita API エンドポイント定義

---

## ✅ マージ判定

> **APPROVE**

issue #8 の完了条件（APIコールが実行でき、レスポンスをパースできること）を満たしている。GsonのKotlin null安全問題に対処するためDTOフィールドを nullable 化しており、Retrofitのデフォルト引数問題も回避済み。`QiitaArticleDtoMapper.toEntity` の空文字列DB混入リスクは後続の issue #9 で `toEntityOrNull` に対処済み。

---

## 📁 変更ファイル一覧

| ファイル | 変更種別 | +行 | -行 | 懸念度 |
|---------|---------|-----|-----|--------|
| `data/remote/api/QiitaApiService.kt` | Added | +26 | - | 🟢 問題なし |
| `data/remote/dto/QiitaArticleDto.kt` | Added | +31 | - | 🟢 問題なし |
| `data/remote/dto/QiitaArticleDtoMapper.kt` | Added | +14 | - | 🟡 軽微（issue9で対処済み） |
| `di/NetworkModule.kt` | Modified | +11 | -3 | 🟢 問題なし |
| `app/build.gradle.kts` | Modified | +2 | -1 | 🟢 問題なし |
| `data/remote/api/QiitaApiServiceTest.kt` | Added | +82 | - | 🟢 問題なし |

---

## 🔍 詳細レビュー

### data/remote/api/QiitaApiService.kt — 🟢 問題なし

#### 変更の意図
Retrofit インターフェースとして `GET /api/v2/items` を定義。

#### 良い点
- `suspend fun` による Coroutine 対応 ✅
- Retrofit のプロキシ経由でデフォルト引数が無効になる問題を回避し、呼び出し側で管理する旨を KDoc に明記 ✅
- `@Query("per_page")` で snake_case を正しく指定 ✅

---

### data/remote/dto/QiitaArticleDto.kt — 🟢 問題なし

#### 変更の意図
Qiita API レスポンスのデータクラス定義。`TagDto` / `UserDto` も同ファイルに集約。

#### 良い点
- GsonのKotlin null安全問題に対処するため全フィールドを nullable 化 ✅
- `tags: List<TagDto> = emptyList()` でAPIが `null` を返した場合のNPEを防止 ✅
- `@SerializedName` で snake_case ↔ camelCase マッピングを明示 ✅
- `TagDto.versions: List<String> = emptyList()` で Qiita API の実構造に対応 ✅
- KDoc で省略フィールドの意図を明記 ✅

---

### data/remote/dto/QiitaArticleDtoMapper.kt — 🟡 軽微

#### 変更の意図
DTO → `ArticleEntity` への変換拡張関数。

#### 指摘事項

**[🟡 m-1] `toEntity` で null フィールドを空文字列にフォールバック** (`QiitaArticleDtoMapper.kt:6-13`)

```kotlin
// ⚠️ 現在のコード: nullのidが空文字列としてDBに混入する可能性
fun QiitaArticleDto.toEntity(status: String, cachedAt: String): ArticleEntity =
    ArticleEntity(
        id = id ?: "",
        title = title ?: "",
        ...
    )
```

```kotlin
// ✅ issue9 で対処済み: 必須フィールドがnullなら null を返す
fun QiitaArticleDto.toEntityOrNull(status: String, cachedAt: String): ArticleEntity? {
    val safeId = id ?: return null
    val safeTitle = title ?: return null
    ...
}
```

> **後続の issue #9 で `toEntityOrNull` に変更済みのため、このPRではマージブロックしない。**

---

### di/NetworkModule.kt — 🟢 問題なし

#### 変更の意図
`QiitaApiService` の Hilt 登録、タイムアウト定数化、`callTimeout` 追加。

#### 良い点
- `TIMEOUT_SECONDS` / `CALL_TIMEOUT_SECONDS` 定数化でマジックナンバー排除 ✅
- `callTimeout(60秒)` 追加でハング防止 ✅
- `BuildConfig.DEBUG` によるログレベル切り替えはリリースビルドでのボディ漏洩を防ぐ ✅
- `provideQiitaApiService` を `@Singleton` で提供し不要なインスタンス生成を防止 ✅

---

### app/build.gradle.kts — 🟢 問題なし

- `BASE_URL` を `"https://qiita.com/"` に更新 ✅
- `mockwebserver` / `kotlinx-coroutines-test` のテスト依存を追加 ✅

---

### data/remote/api/QiitaApiServiceTest.kt — 🟢 問題なし

#### 変更の意図
MockWebServer を使った `QiitaApiService` の統合テスト。

#### 良い点
- 正常系・空 tags の2ケースをカバー ✅
- `@Before`/`@After` で MockWebServer のライフサイクルを適切に管理 ✅
- `runTest` による Coroutine テスト ✅

---

## ⚠️ 影響範囲

**このPRの変更が影響する箇所**:
- `di/NetworkModule.kt` — `QiitaApiService` を Hilt グラフに追加、後続の ViewModel/Repository から注入可能
- `data/remote/dto/QiitaArticleDtoMapper.kt` — issue #9 で `toEntityOrNull` に変更済み

**破壊的変更**: なし（新規追加のみ）

---

## 🧪 テスト確認

| テスト項目 | 状態 |
|-----------|------|
| 既存テストへの影響 | ✅ 既存ファイルへの破壊的変更なし |
| 新機能のテスト | ✅ `QiitaApiServiceTest` 追加済み |
| 正常レスポンスのパース | ✅ `getItems_正常レスポンスをパースできる` |
| 空 tags のパース | ✅ `getItems_空のtagsをパースできる` |
| API エラー時のテスト | ❓ 未追加（将来対応推奨） |

---

## 💬 レビューコメント（コピペ用）

**全体コメント**:
```
レビューしました。APPROVE です 👍

Qiita API エンドポイントの定義として必要な要素が揃っています。
GsonのKotlin null安全問題への対処（全フィールドnullable化）や
Retrofitのデフォルト引数問題の回避など、実装上のトレードオフが
KDocに明記されている点が良いです。

QiitaArticleDtoMapper の空文字列フォールバック問題は
issue #9 で toEntityOrNull に対処済みです。

詳細は docs/reviews/pr_review_issue8_20260303.md を参照してください。
```

---

## ✅ チェックリスト

- [x] `GET /api/v2/items` エンドポイントが定義されている
- [x] `QiitaArticleDto` / `TagDto` / `UserDto` データクラスが作成されている
- [x] GsonのKotlin null安全問題に対処（nullable化 + tags デフォルト値）
- [x] `NetworkModule` に `QiitaApiService` が登録されている
- [x] `MockWebServer` を使った統合テストが追加されている
- [x] タイムアウト値が定数化されている
- [x] `BASE_URL` が `https://qiita.com/` に更新されている

---
*Generated by Claude Code / pr-review skill*
